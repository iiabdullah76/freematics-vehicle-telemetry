# Freematics Vehicle Telemetry (ESP32 / OBD-II / GPS / LTE)

Real-time vehicle telemetry using **Freematics ONE+ (ESP32)**.  
Streams **OBD-II**, **GPS**, and optional **LTE** data to an HTTP/WebSocket backend and displays it on a web dashboard.

> **Stack:** ESP32 (Freematics) firmware · Python backend (HTTP + WebSocket) · HTML/CSS/JS dashboard

---

## ✨ Features
- OBD-II metrics (speed, RPM, coolant temp, etc.)
- GPS (lat/lon, speed, heading)
- LTE-ready (send over mobile network)
- HTTP ingestion + WebSocket live updates
- Clean web dashboard with live charts
- API spec & sample payloads

---

## 🗂 Repository Structure
